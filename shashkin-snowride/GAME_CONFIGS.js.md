// =====================================
// ПРИМЕРЫ РАЗЛИЧНЫХ КОНФИГУРАЦИЙ ИГРЫ
// Скопируйте нужные настройки в GAME_CONFIG в index.html
// =====================================

// 🎮 КОНФИГУРАЦИЯ ДЛЯ НАЧИНАЮЩИХ (ОЧЕНЬ ЛЁГКАЯ)
const BEGINNER_CONFIG = {
    collisionBuffer: {
        playerBuffer: 15,           // Большой буфер для игрока
        obstacleBuffer: 8,          // Большой буфер для препятствий
        enablePlayerBuffer: true,
        enableObstacleBuffer: true,
        customBuffers: {
            snowHeap: 12,            // Очень большой буфер
            snowball: 15,            // Максимальный буфер для снежков
            tree: 10,               // Большой буфер для деревьев
            hare: 18,               // Очень большой буфер для зайцев
            rock: 8,                // Большой буфер для камней
            log: 12                 // Большой буфер для брёвен
        }
    },
    difficulty: {
        initialObstacleChance: 0.008,   // Очень редкие препятствия
        maxObstacleChance: 0.02,         // Максимум 2%
        initialMinDistance: 600,         // Большие расстояния
        finalMinDistance: 350            // Конечное расстояние
    },
    speedIncreaseRate: 0.00008,          // Очень медленное усложнение
    jumpForce: {
        short: -14,                      // Более сильный короткий прыжок
        long: -20                        // Очень сильный длинный прыжок
    }
};

// 🎯 КОНФИГУРАЦИЯ ДЛЯ ОПЫТНЫХ ИГРОКОВ (СРЕДНЯЯ)
const INTERMEDIATE_CONFIG = {
    collisionBuffer: {
        playerBuffer: 8,               // Умеренный буфер
        obstacleBuffer: 4,            // Стандартный буфер
        enablePlayerBuffer: true,
        enableObstacleBuffer: true,
        customBuffers: {
            snowHeap: 6,
            snowball: 8,
            tree: 4,
            hare: 10,
            rock: 3,
            log: 5
        }
    },
    difficulty: {
        initialObstacleChance: 0.015,  // Стандартная частота
        maxObstacleChance: 0.035,      // Стандартный максимум
        initialMinDistance: 400,        // Стандартное расстояние
        finalMinDistance: 200          // Стандартное конечное расстояние
    },
    speedIncreaseRate: 0.0002,         // Стандартное усложнение
    jumpForce: {
        short: -12,
        long: -18
    }
};

// 🔥 КОНФИГУРАЦИЯ ДЛЯ ЭКСПЕРТОВ (СЛОЖНАЯ)
const EXPERT_CONFIG = {
    collisionBuffer: {
        playerBuffer: 4,               // Маленький буфер
        obstacleBuffer: 2,            // Минимальный буфер
        enablePlayerBuffer: true,
        enableObstacleBuffer: true,
        customBuffers: {
            snowHeap: 3,
            snowball: 5,
            tree: 2,
            hare: 6,
            rock: 1,
            log: 3
        }
    },
    difficulty: {
        initialObstacleChance: 0.02,   // Частые препятствия
        maxObstacleChance: 0.045,     // Высокая частота
        initialMinDistance: 300,       // Близкие расстояния
        finalMinDistance: 180         // Очень близкие расстояния
    },
    speedIncreaseRate: 0.00035,        // Быстрое усложнение
    jumpForce: {
        short: -11,
        long: -17
    }
};

// 💀 КОНФИГУРАЦИЯ "ХАРДКОР" (МАКСИМАЛЬНАЯ СЛОЖНОСТЬ)
const HARDCORE_CONFIG = {
    collisionBuffer: {
        playerBuffer: 0,              // БУФЕРЫ ОТКЛЮЧЕНЫ!
        obstacleBuffer: 0,
        enablePlayerBuffer: false,   // Полностью отключено
        enableObstacleBuffer: false, // Полностью отключено
        customBuffers: {
            snowHeap: 0,
            snowball: 0,
            tree: 0,
            hare: 0,
            rock: 0,
            log: 0
        }
    },
    difficulty: {
        initialObstacleChance: 0.025, // Очень частые препятствия
        maxObstacleChance: 0.06,     // Максимальная частота
        initialMinDistance: 250,      // Очень близко
        finalMinDistance: 150        // Предельно близко
    },
    speedIncreaseRate: 0.0005,        // Экстремальное усложнение
    jumpForce: {
        short: -10,                   // Слабее прыжки
        long: -16
    }
};

// 📱 КОНФИГУРАЦИЯ ДЛЯ МОБИЛЬНЫХ УСТРОЙСТВ
const MOBILE_CONFIG = {
    collisionBuffer: {
        playerBuffer: 12,             // Большой буфер для точности пальцев
        obstacleBuffer: 6,           // Увеличенный буфер
        enablePlayerBuffer: true,
        enableObstacleBuffer: true,
        customBuffers: {
            snowHeap: 8,
            snowball: 10,
            tree: 6,
            hare: 12,
            rock: 5,
            log: 7
        }
    },
    difficulty: {
        initialObstacleChance: 0.012, // Чуть реже препятствий
        maxObstacleChance: 0.03,     // Умеренная частота
        initialMinDistance: 450,     // Чуть больше расстояния
        finalMinDistance: 220
    },
    speedIncreaseRate: 0.00015,       // Умеренное усложнение
    jumpForce: {
        short: -14,                  // Более сильные прыжки
        long: -20                    // Для компенсации точности управления
    },
    maxJumpHoldTime: 50              // Более отзывчивое управление
};

// ⚡ КОНФИГУРАЦИЯ ДЛЯ СЛАБЫХ УСТРОЙСТВ
const LOW_PERFORMANCE_CONFIG = {
    collisionBuffer: {
        playerBuffer: 10,            // Большой буфер для компенсации FPS
        obstacleBuffer: 5,
        enablePlayerBuffer: true,
        enableObstacleBuffer: true,
        customBuffers: {
            snowHeap: 7,
            snowball: 9,
            tree: 5,
            hare: 11,
            rock: 4,
            log: 6
        }
    },
    // Уменьшаем визуальные эффекты для производительности
    starCount: 100,                  // Меньше звёзд
    mountainLayers: 2,               // Меньше слоёв гор
    mountainsPerLayer: 4,           // Меньше гор
    snowmobileSprite: {
        frameDuration: 60           // Меньше FPS (16 вместо 25)
    }
};

// =====================================
// ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ:
// =====================================

/*
1. ВЫБЕРИТЕ подходящую конфигурацию выше

2. СКОПИРУЙТЕ нужные секции в ваш GAME_CONFIG в index.html:

   Пример для новичка:
   В вашем index.html найдите секцию collisionBuffer и замените её на:
   
   collisionBuffer: BEGINNER_CONFIG.collisionBuffer,
   
   Или просто скопируйте значения из нужной конфигурации.

3. ОСНОВНЫЕ ИЗМЕНЕНИЯ для разных уровней:
   
   • БУФЕРЫ: больше = легче, меньше = сложнее
   • obstacleChance: больше = чаще препятствия
   • minDistance: меньше = ближе препятствия  
   • speedIncreaseRate: больше = быстрее усложняется
   • jumpForce: больше по модулю = сильнее прыжки

4. ТЕСТИРОВАНИЕ:
   Изменяйте параметры постепенно, тестируя результат.
   Лучше всего изменять по одному параметру за раз.

5. ПРОИЗВОДСТВЕННОСТЬ:
   Если игра тормозит, используйте LOW_PERFORMANCE_CONFIG
   или уменьшите визуальные эффекты вручную.
*/