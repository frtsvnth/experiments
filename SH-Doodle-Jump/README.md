# 🎮 Doodle Jump Clone

Полнофункциональная мобильная казуальная игра в стиле **Doodle Jump** с поддержкой PWA (Progressive Web App).

## 📝 Описание

Прыгай как можно выше! Управляй зелёным персонажем, прыгая по платформам и избегая падения. Игра создана с использованием чистого JavaScript и Canvas API, полностью адаптирована для мобильных устройств и может быть установлена как PWA приложение.

## ✨ Реализованные возможности

### Игровая механика
- ✅ **Физика прыжков** с гравитацией и инерцией
- ✅ **Процедурная генерация платформ** с возрастающей сложностью
- ✅ **4 типа платформ**:
  - 🟢 **Обычные** (серые) - стандартные платформы
  - 🔵 **Движущиеся** (синие) - перемещаются влево-вправо
  - 🔴 **Хрупкие** (красные) - разрушаются после прыжка
  - 🟩 **Пружинные** (зелёные) - супер-прыжок!
- ✅ **Система частиц** для визуальных эффектов
- ✅ **Счёт** и сохранение рекорда в localStorage
- ✅ **Плавная камера**, следующая за игроком

### Управление
- ⌨️ **Desktop**: Стрелки ← → для движения
- 📱 **Mobile**: Касание левой/правой части экрана

### Интерфейс
- ✅ Стартовое меню с отображением рекорда
- ✅ Экран Game Over с результатами
- ✅ Счётчик очков во время игры
- ✅ Адаптивный дизайн для всех устройств

### PWA функции
- ✅ Работает офлайн (Service Worker)
- ✅ Устанавливается на главный экран
- ✅ Полноэкранный режим на мобильных
- ✅ Оптимизация для iOS (apple-mobile-web-app)

## 🚀 Быстрый старт

### Локальный запуск

1. Клонируйте репозиторий или скачайте файлы
2. Откройте `index.html` в браузере

**Важно**: Для работы Service Worker требуется HTTPS или localhost. Для локального тестирования используйте:

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js (если установлен http-server)
npx http-server -p 8000
```

Затем откройте `http://localhost:8000` в браузере.

## 🌐 Деплой на GitHub Pages

### Пошаговая инструкция

1. **Создайте репозиторий на GitHub**
   - Перейдите на github.com
   - Нажмите "New repository"
   - Назовите его, например, `doodle-jump-game`

2. **Загрузите файлы**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Doodle Jump game"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/doodle-jump-game.git
   git push -u origin main
   ```

3. **Настройте GitHub Pages**
   - Зайдите в Settings → Pages
   - Source: Deploy from a branch
   - Branch: main → / (root) → Save

4. **Создайте иконки (см. ниже)**

5. **Готово!** Игра будет доступна по адресу:
   ```
   https://YOUR_USERNAME.github.io/doodle-jump-game/
   ```

## 🎨 Создание иконок для PWA

Для полноценной работы PWA нужны иконки. Вы можете создать их несколькими способами:

### Вариант 1: Использование онлайн-генераторов

1. Откройте [Favicon Generator](https://www.favicon-generator.org/) или [PWA Asset Generator](https://www.pwabuilder.com/imageGenerator)
2. Загрузите любое изображение или создайте простую иконку
3. Скачайте сгенерированные иконки размером 192x192 и 512x512
4. Переименуйте в `icon-192.png` и `icon-512.png`
5. Поместите в корневую папку проекта

### Вариант 2: Использование DALL-E или другого AI

**Промпт для генерации**:
```
2D pixel art green alien character for mobile game,
square format, transparent background,
simple and cute design, jumping pose,
64x64 pixels style, doodle jump inspired
```

После генерации:
1. Скачайте изображение
2. Используйте онлайн-редактор (например, [Squoosh.app](https://squoosh.app/))
3. Измените размер до 192x192 и 512x512
4. Сохраните как PNG

### Вариант 3: Создание простой иконки вручную

Используйте любой графический редактор:
1. Создайте квадратное изображение
2. Нарисуйте простого зелёного персонажа
3. Экспортируйте в размерах 192x192 и 512x512
4. Формат: PNG с прозрачным фоном

### Быстрое решение (временное)

Создайте простые цветные квадраты с помощью CSS/Canvas и сохраните как скриншоты:

```html
<!-- Создайте временный HTML файл -->
<div style="width:192px;height:192px;background:#4CAF50;display:flex;align-items:center;justify-content:center;font-size:80px;color:white;border-radius:30px;">🎮</div>
```

Сделайте скриншот и сохраните.

## 📱 Тестирование PWA на iOS

1. Откройте игру в Safari на iPhone/iPad
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой)
3. Выберите "На экран «Домой»"
4. Игра установится как приложение
5. Запустите с домашнего экрана

На Android:
1. Откройте в Chrome
2. Нажмите меню (три точки)
3. Выберите "Установить приложение" или "Add to Home Screen"

## 📂 Структура проекта

```
doodle-jump-game/
│
├── index.html          # Основная HTML страница
├── style.css           # Стили и адаптивность
├── script.js           # Вся игровая логика
├── manifest.json       # PWA манифест
├── sw.js              # Service Worker для офлайн режима
├── icon-192.png       # Иконка 192x192 (создайте)
├── icon-512.png       # Иконка 512x512 (создайте)
└── README.md          # Этот файл
```

## 🎯 Технические детали

### Параметры игры
- Размер canvas: 480x800px (адаптивный)
- Гравитация: 0.6
- Скорость прыжка: -15 (обычная), -22 (пружинная)
- Скорость персонажа: 8
- Размер персонажа: 40x40px
- Размер платформ: 80x15px

### Цветовая схема
- Фон: Градиент от #87CEEB к #E0F7FA
- Персонаж: #4CAF50 (зелёный)
- Акцент (кнопки): #FF6B6B (красный)
- Платформы:
  - Обычная: #808080 (серый)
  - Движущаяся: #2196F3 (синий)
  - Хрупкая: #F44336 (красный)
  - Пружинная: #8BC34A (зелёный)

### Оптимизации
- Retina-дисплеи поддерживаются (автоматический scaling)
- Удаление невидимых платформ для производительности
- Процедурная генерация только нужных объектов
- Кэширование ресурсов через Service Worker

## 🔧 Настройка и модификация

### Изменение сложности
В файле `script.js` найдите константы в начале файла:

```javascript
const GRAVITY = 0.6;           // Увеличьте для быстрого падения
const JUMP_VELOCITY = -15;     // Увеличьте для выше прыжков
const PLAYER_SPEED = 8;        // Скорость движения персонажа
```

### Добавление своего спрайта персонажа

1. Создайте изображение персонажа (64x64px рекомендуется)
2. Сохраните как `player.png` в корне проекта
3. В `script.js` найдите функцию `draw()` и замените отрисовку прямоугольника на:

```javascript
// Вместо ctx.fillRect(player.x, screenY, player.width, player.height);
const playerImage = new Image();
playerImage.src = './player.png';
ctx.drawImage(playerImage, player.x, screenY, player.width, player.height);
```

### Изменение цветов
Все цвета определены в объекте `COLORS` в начале `script.js`.

## 🐛 Известные ограничения

- Иконки нужно создать вручную (они не включены в репозиторий)
- На некоторых старых браузерах PWA может не работать
- iOS требует добавления на домашний экран вручную (нет автоматического промпта)

## 🤝 Вклад

Это учебный проект. Вы можете:
- Форкнуть репозиторий
- Добавить новые типы платформ
- Улучшить графику
- Добавить звуковые эффекты
- Реализовать систему пауэр-апов

## 📄 Лицензия

Свободное использование в учебных целях.

## 🎮 Приятной игры!

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

---

**Создано с ❤️ для изучения веб-разработки и создания игр**
